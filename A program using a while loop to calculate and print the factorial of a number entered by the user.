#include <stdio.h>

int main() {
    int n, temp;
    long long factorial = 1;
    scanf("%d", &n);
    
    temp = n;
    while (temp > 1) {
        factorial *= temp;
        temp--;
    }
    
    printf("Factorial of %d = %lld\n", n, factorial);
    return 0;
}
