#include <stdio.h>
#include <string.h>

int main() {
    char str1[100], str2[50], result[150];
    
    scanf("%s", str1);
    scanf("%s", str2);
    
    strcpy(result, str1);
    strcat(result, str2);
    
    printf("Combined string:\n%s\n", result);
    
    return 0;
}
